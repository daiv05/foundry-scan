# PocketBase admin credentials
# Used by docker-compose to initialize the PocketBase admin account on first run.
# Must match the values in backend/.env — the backend uses them to authenticate against PocketBase.
PB_ADMIN_EMAIL=admin@example.com
PB_ADMIN_PASSWORD=changeme
